/**
 * Created by ronnygeo on 2/13/16.
 */

(function(){
    // var theater = theaterJS(); // Commented out - Moved to About.js

    // theater // Commented out - Moved to About.js
    //     .addActor('role', {speed: 0.6, accuracy: 0.8})
    //     .addScene('role:AI', 600)
    //     .addScene('role:VISION', 500)
    //     .addScene('role:NLP', 500)
    //     .addScene('role:DATA', 700)
    //     .addScene('role:ANALYTICS')
    //     .addScene('role: WEB', 400)
    //     .addScene('role: SOFTWARE', 400)
    //     .addScene(theater.replay.bind(theater)); // Commented out - Moved to About.js

    // window.sr = ScrollReveal({reset: true}); // Commented out - Moved to App.js
    
    // Specific sr.reveal calls are being moved to their respective components.
    // if (window.sr) {
        // sr.reveal('#intro', {duration: 500}); // Moved to About.js
        // sr.reveal('#education', {duration: 500}); // Element not in current components
        // sr.reveal('#experience', {duration: 1000}); // Element not in current components
        // sr.reveal('#projects', {duration: 500}); // Moved to Portfolio.js
        // sr.reveal('#image-thumb-1', {duration: 100}); // Element not in current components (Interests)
        // sr.reveal('#image-thumb-2', {duration: 300}); // Element not in current components (Interests)
        // sr.reveal('#image-thumb-3', {duration: 500}); // Element not in current components (Interests)
        // sr.reveal('#image-thumb-4', {duration: 700}); // Element not in current components (Interests)
    // }
})();

// $(document).foundation(); // Commented out - Moved to App.js

// $('#offCanvasLeft').click(function(){ // Commented out - Moved to Header.js
//     $('#offCanvasLeft').removeClass('is-open');
//     $('#offCanvasLeft').attr('aria-hidden',true);
//     $('#offCanvasWrapper').removeClass('is-off-canvas-open').removeClass('is-open-left');
// });

// $('a').smoothScroll(); // Commented out - Moved to App.js
